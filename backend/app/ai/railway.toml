[deploy]
startCommand = "uvicorn serve:app --host 0.0.0.0 --port $PORT --log-level info"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
